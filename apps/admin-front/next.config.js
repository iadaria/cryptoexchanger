/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  /*   experimental: {
    swcPlugins: [["effector/babel-plugin", {}]],
  }, */
};

module.exports = nextConfig;
