export enum ExchangeType {
  sell = "sell",
  buy = "buy",
}

export enum ExchangeStatus {
  Done = "done",
  Active = "active",
  Rejected = "rejected",
  Timeout = "timeout",
  Cancelled = "cancelled",
}
